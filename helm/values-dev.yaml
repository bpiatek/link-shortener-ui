replicaCount: 1

spring:
  profiles:
    active: "dev"
env:
  - name: JAVA_TOOL_OPTIONS
    value: "-XX:MaxRAMPercentage=75.0 -XX:+UseG1GC"

vault:
  role: link-shortener-ui-dev-role
  secretPath: kv/data/dev/link-shortener-ui/config
  secretTemplate: |
    {{- with secret "kv/data/dev/link-shortener-ui/config" -}}
    {{- range $key, $value := .Data.data }}
    {{ $key }}={{ $value }}
    {{- end }}
    {{- end }}